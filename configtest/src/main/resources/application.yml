server:
  port: 9000
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/caijinkun/springcloudtest/
          search-paths:
          - git-config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/